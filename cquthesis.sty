\ProvidesPackage{cquthesis}[2016/05/09 The place for your beloved packages and customization.]

%=====你可以在这里导入想用的Package，定义你自己的功能和常量=======
\newcommand{\speakyourlove}[1][Mom]{\emph{#1, I Love You!}}













%========一些推荐功能=========
% 用双引号括起文字
\newcommand{\qthis}[1]{\textquotedblleft{}#1\textquotedblright{}}
%交叉引用图X，方程X：
\newcommand{\refimg}[1]{图\ref{#1}}
\newcommand{\reftab}[1]{表\ref{#1}}
\newcommand{\refeq}[1]{式\ref{#1}}
% 表格行数序号计数器
\newcounter{RowNum}
\newcommand{\rownum}{
	\stepcounter{RowNum}
	\theRowNum
}
% 行数计数器清零
\newcommand{\resetrownum}{\setcounter{RowNum}{0}}
%================================
%**********插件定义区域***********
%================================
% 很棒的化学式和方程支持
\RequirePackage[version=4]{mhchem}
% 在命令里使用逗号快速表达多个化学式\cec{H2O,HF,HCl}
\newcommand{\cec}[1]{\cesplit{{\,\ }{\0}}{#1}}
%si单位支持
\usepackage{siunitx}
\sisetup{	list-final-separator	=	{和},
	list-pair-separator		=	{和},
	range-phrase			=	{$ \sim $},
}
% 处理数学公式中的黑斜体的宏包
% \RequirePackage{bm}

% 不同于 \mathcal \mathfrak 之类的英文花体字体
% \RequirePackage{mathrsfs}

% 浮动图形控制宏包。
% 允许上一个 section 的浮动图形出现在下一个 section 的开始部分
% 该宏包提供处理浮动对象的 \FloatBarrier 命令，使所有未处
% 理的浮动图形立即被处理。这三个宏包仅供参考，未必使用：
% \RequirePackage[below]{placeins}
% \RequirePackage{floatflt} % 图文混排用宏包
% \RequirePackage{rotating} % 图形和表格的控制旋转

% 给自定义的宏后面自动加空白
% \RequirePackage{xspace}

% 借用 ltxdoc 里面的几个命令。
\def\cmd#1{\cs{\expandafter\cmd@to@cs\string#1}}
\def\cmd@to@cs#1#2{\char\number`#2\relax}
\DeclareRobustCommand\cs[1]{\texttt{\char`\\#1}}

\newcommand*{\meta}[1]{{%
		\ensuremath{\langle}\rmfamily\itshape#1\/\ensuremath{\rangle}}}
\providecommand\marg[1]{%
	{\ttfamily\char`\{}\meta{#1}{\ttfamily\char`\}}}
\providecommand\oarg[1]{%
	{\ttfamily[}\meta{#1}{\ttfamily]}}
\providecommand\parg[1]{%
	{\ttfamily(}\meta{#1}{\ttfamily)}}
\providecommand\pkg[1]{{\sffamily#1}}