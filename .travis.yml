os:
  - linux

sudo: required

language: python
python:
	- "2.7"

install:
 - source ./toolkit/texlive.sh
 - make cls
 - source ./toolkit/dependencyresort.sh

cache:
  directories:
    - /tmp/texlive
    - $HOME/.texlive

script:
  - make doc
  - make main